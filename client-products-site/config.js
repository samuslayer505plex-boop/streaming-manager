// Configuración de la app — reemplaza valores antes de desplegar
window.APP_CONFIG = {
  // URL pública del endpoint de productos
  API_URL: 'https://api.example.com/products',
  // Esquema de autorización solicitado: 'm+' según indicación
  AUTH_SCHEME: 'm+',
  // Deja vacío y pon el token aquí antes de desplegar
  TOKEN: '',
  // Mostrar imágenes si la API devuelve `img` (true/false)
  USE_IMAGES: true
};
